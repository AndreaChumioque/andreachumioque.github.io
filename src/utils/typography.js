import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  headerFontFamily: [
    'TTNormsBold',
    'sans-serif',
  ],
  bodyFontFamily: ['TTNormsRegular', 'sans-serif'],
});

export default typography;
